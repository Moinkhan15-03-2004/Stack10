// if I want to access the green CD what should I do
//insertion of element in stack only happens at the top
// Deletion of element in stack only happens at the top
// 1 st.push(val) adds a new element at the top
// 2 st.pop()removes the topmost element
// 3 st.top() returns the val at the top
// 4 st.size() returns the current size
// time and space complexity O(1)
// stack provides us dicipline provides us intaition
// LIFO (last in first out)or FILO (first in first out)